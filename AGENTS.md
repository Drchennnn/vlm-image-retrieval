# Project Guidance for Codex CLI

## 项目定位

本项目基于预训练视觉语言模型，研究双向图文检索：

- 文本到图像检索（text-to-image）
- 图像到文本检索（image-to-text）

项目重点包括理解对比预训练如何将图像和文本映射到共享表示空间。开发顺序是先实现可信、可复现的基线，再根据验证结果决定是否进行微调或其他改进。

## 协作约定

Codex 作为审慎、清晰的机器学习开发伙伴与我协作：

- 日常沟通使用中文。
- 项目 `README.md` 默认使用英文。
- 明确区分论文事实、设计选择、已验证结果和假设。
- 不虚构项目进度、实验结果、指标、运行记录或数据状态。
- 报告进度时以现场检查、代码、日志和测试结果为依据。

## 工作规则

- 日常创建或修改 `README.md` 可以直接执行，并应保持内容小而可核查。
- 涉及大批量删除文件时，必须先说明准确删除范围并等待我明确批准。
- 不得因全局“自主执行”指令而跳过大批量删除的审批规则。
- 创建目录、下载数据、训练模型或引入新依赖前，先确认这些动作属于当前任务范围。
- 保持修改小而可核查；完成重要改动后进行适当验证。
- 不创建与当前任务无关的文件夹或文件。

## 当前进度（现场检查）

截至 2026-09-17：

- 根目录已有 `README.md`，其内容将项目描述为规划阶段。
- 根目录已有两篇论文 PDF：
  - `Learning Transferable Visual Models From Natural Language Supervision.pdf`
  - `Winoground.pdf`
- 当前可见的工具或工作流目录包括 `.agents`、`.specify`、`openspec` 和 `.omx`。
- 当前未发现项目代码文件。
- 当前未发现数据文件或数据目录。
- 尚未验证任何模型运行、数据处理、检索结果、实验指标或训练结果。
- README 中提出的目录结构、模型、数据集、指标和命令均属于规划内容，不应视为已完成事实。

## 对话摘要与已确定方向

- 使用 Codex CLI 作为主要协作工具。
- README 先行，用于记录项目定位、方法概述和已确认信息。
- 论文内容只在 README 中作简要说明；必要时再创建单独笔记。
- 此前讨论过 build-spec、OpenSpec 和 Spec Kit，但本项目不会因为这些工具已安装而自动启动任何工作流。
- 仅记录影响项目的决定、进度和验证结果，不保存逐字聊天记录、个人信息或推测。

## 下一步

下一步是确定可信基线所需的检索数据集，并核对其访问方式、许可证、数据划分和评估可行性；在明确任务范围前不下载数据。完成重要里程碑后，更新本文件中的进度与决策摘要；README 的逐次展示和审批规则始终有效。
